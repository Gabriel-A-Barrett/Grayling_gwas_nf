process combineEnv {
    publishDir 
    cache 'lenient'
    
    container 'gbarrett9086/gwas-nf:latest'

    input:
    path (outliers)

    output:
    //path ("*.txt"), emit: combine
    stdout emit: stdout

    script:
    """
    combineEnv.r "${files.collect {"${it}"}.print()}"
    """
}